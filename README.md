# Gamification React App

## Install
```bash
npm install
```

## Development
```bash
npm run dev
```

## Deploy to GitHub Pages
```bash
npm run deploy
```

The `base` option in `vite.config.js` is set to `/gamification/` so the app works when deployed on GitHub Pages.
